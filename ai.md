# Work with AI

## First generated spec.md

AI generated a `spec.md` in which entity `Favourite` was provided, but `README.md` consist information, that users can listen and manage their favourite tracks, but cannot mark one or multiple favourite tracks as `favourite`.
There is no feature `favourite`. `spec.md` should be redefined.
